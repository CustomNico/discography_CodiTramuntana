<p id="notice"><%= notice %></p>

<h1>Artist information</h1>

<p>
  <strong>Name:</strong>
  <%= @artist.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @artist.description %>
</p>

<p>
  <strong>Total of LPs:</strong>
  <%= @lps.length %>
</p>

<br>
<%= link_to 'List of LPs', records_artist_path(@artist), class: "btn btn-large btn-primary" %> |
<%= link_to 'Edit artist', edit_artist_path(@artist), class: "btn btn-large btn-warning" %> |
<%= link_to 'Back', artists_path, class: "btn btn-large btn-info" %>
